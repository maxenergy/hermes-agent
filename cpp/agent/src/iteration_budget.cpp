#include "hermes/agent/iteration_budget.hpp"

// IterationBudget is fully header-defined; this TU exists so the
// CMakeLists has something to compile and the symbol table records the
// type for downstream debuggers.
namespace hermes::agent {
// Intentionally empty.
}  // namespace hermes::agent
